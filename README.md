## download the source code and copy the path of the folder you installed it in.
## open it in command prompt and then run "py -m pip install -r requirements.txt", and after that run the python file named "BiomeMacro.py" as **ADMINISTRATOR** ok bye

also if it somehow wasn't obvious you need to install python for this to work
# make sure to run the python file as ADMINISTRATOR for it to work
