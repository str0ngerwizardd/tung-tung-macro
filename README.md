just like download the source code and go to the folder in command prompt and then run "py -m pip install -r requirements.txt" and after that run the python file named "BiomeMacro.py" ok bye 

also if it somehow wasn't obvious you need to install python for this to work
